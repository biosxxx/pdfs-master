# PDF Master

`PDF Master` is bundled as a standalone React + Vite sub-application and deployed to `/utilities/pdf-master/`.

Use the embedded route from the portal shell or open the standalone tool directly.
